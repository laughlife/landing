DATABASE_URL="mysql://USER:URL_ENCODED_PASSWORD@localhost:8866/wysm"

# Generate a random value of at least 64 characters before deployment.
NUXT_SESSION_PASSWORD="replace-with-at-least-64-random-characters"
ADMIN_INITIAL_USERNAME="admin"
ADMIN_INITIAL_PASSWORD="replace-with-a-strong-random-password"

UPLOAD_DIR="./storage/uploads"

NITRO_HOST="0.0.0.0"
NITRO_PORT=4000
# Replace localhost with the production domain or public address before deployment.
NUXT_PUBLIC_SITE_URL="http://localhost:4000"
NUXT_PUBLIC_SITE_NAME="南阳市吴月商贸行"
TRUST_PROXY=false
